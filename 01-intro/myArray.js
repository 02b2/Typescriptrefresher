"use strict";
exports.__esModule = true;
var superHeros = []; // defines an array of type string then creates and empty array
var heroPower = []; // this also defines an array of type string and creates an empty array but slightly different syntax
var allUsers = [];
superHeros.push("superman");
heroPower.push(2);
allUsers.push({ name: "", isActive: true });
