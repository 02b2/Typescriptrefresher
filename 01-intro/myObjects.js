"use strict";
exports.__esModule = true;
var User = {
    name: "Pedro",
    email: "@pedro",
    isActive: true
};
function createUser(_a) {
    var string = _a.name, boolean = _a.isPaid;
}
var newUser;
({ name: "pedro", isPaid: false, email: "@pedro" });
createUser(newUser);
function createCourse() {
    return { name: "react", price: 399 };
}
