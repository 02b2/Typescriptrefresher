const User = {
    name: "Pedro",
    email: "@pedro",
    isActive: true

}

function createUser({name: string, isPaid: boolean}) {}

let newUser({name: "pedro", isPaid: false, email: "@pedro"})

createUser(newUser)


export {}